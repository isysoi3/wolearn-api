version: "3.3"

services:

  api:
    build:
      context: .
      dockerfile: web.Dockerfile
    image: isysoi/wolearn_api
    environment:
      ENVIRONMENT: dev
    ports:
      - 8080:80
  #   depends_on:
  #     - "db"

  # db:
  #   image: postgres
  #   environment:
  #     POSTGRES_USER: test
  #     POSTGRES_PASSWORD: test
  #     POSTGRES_DB: test